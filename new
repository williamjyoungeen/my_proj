# Запрашиваем у пользователя количество чисел, которые он хочет ввести-
num_count = int(input("Введите количество чисел для суммирования:"))

# Инициализируем переменную для хранения суммы/
total_sum = 0

# Запрашиваем у пользователя числа и суммируем их
for i in range(num_count):
    number = float(input(f"Введите число {i+1}: "))
    total_sum += number

# Выводим результат
print(f"Сумма введенных  чисел: {total_sum}")
